# Waybar с встроенным модулем cava (заменяет обычный waybar)
waybar-cava

# Генерация цветов из обоев — ОСНОВНОЙ генератор акцентов (реальные цвета
# пикселей обоев): kitty/waybar/rofi/swaync/eww/btop/starship/cava/hyprlock/
# gtk/spicetify/sddm/obs/рамка окна + Firefox (pywalfox). matugen = структура.
wallust

# Темизация Firefox под обои: wallust пишет ~/.cache/wal/colors.json (pywal-
# формат), pywalfox отдаёт цвета расширению. После установки выполнить раз:
# `pywalfox install` (регистрирует native-messaging host) + поставить
# расширение pywalfox в Firefox.
python-pywalfox

# GUI для выбора обоев
waypaper

# Тема экрана входа SDDM (Qt6, видео-фон + перекраска под matugen)
sddm-astronaut-theme

# Темизация Spotify
spicetify-cli

# Шрифты Red Hat
redhat-fonts

# Приложения с биндами (Super+H, Super+V)
happ-desktop-bin
visual-studio-code-bin

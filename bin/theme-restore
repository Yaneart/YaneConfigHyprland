#!/usr/bin/env bash
set -euo pipefail

STATE_FILE="$HOME/.local/state/theme/current_wallpaper"
FALLBACK="$HOME/.config/background"

if [[ -f "$STATE_FILE" ]]; then
  file="$(<"$STATE_FILE")"
  if [[ -f "$file" ]]; then
    exec "$HOME/.local/bin/theme-apply" "$file"
  fi
fi

if [[ -f "$FALLBACK" ]]; then
  exec "$HOME/.local/bin/theme-apply" "$FALLBACK"
fi

notify-send "Theme restore skipped" "No saved wallpaper found"
